name: 'Set output variable'

outputs:
  name:
    description: 'The best whale shark'
    value: ${{ steps.set-output.outputs.name }}

runs:
  using: 'composite'
  steps:
    - id: set-output
      shell: bash
      run: ./.github/actions/output/set-output.sh
